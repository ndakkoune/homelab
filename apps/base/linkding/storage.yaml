apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-storage
  namespace: linkding
  annotations:
    volumeType: local
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 512Mi
