apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  chart:
    spec:
      chart: cert-manager
      version: v1.18.x
      interval: 1h
      sourceRef:
        kind: HelmRepository
        name: cert-manager
  interval: 5m
  values:
    crds:
      enabled: true
